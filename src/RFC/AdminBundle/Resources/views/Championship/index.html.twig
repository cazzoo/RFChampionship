{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Championship list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Isagreed</th>
                <th>Listevents</th>
                <th>Listmanagers</th>
                <th>Metarule</th>
                <th>Listrules</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_championship_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.isAgreed }}</td>
                <td>{{ entity.listEvents }}</td>
                <td>{{ entity.listManagers }}</td>
                <td>{{ entity.metaRule }}</td>
                <td>{{ entity.listRules }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_championship_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_championship_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_championship_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}

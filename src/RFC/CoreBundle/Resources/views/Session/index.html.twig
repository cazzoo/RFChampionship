{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Session list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Begindate</th>
                <th>Enddate</th>
                <th>Type</th>
                <th>Listresults</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_session_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.beginDate %}{{ entity.beginDate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.endDate %}{{ entity.endDate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.type }}</td>
                <td>{{ entity.listResults }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_session_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_session_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_session_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}

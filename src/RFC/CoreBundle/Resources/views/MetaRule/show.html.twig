{% extends '::base.html.twig' %}

{% block body -%}
    <h1>MetaRule</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Listrules</th>
                <td>{{ entity.listRules }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_metaRule') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_metaRule_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
